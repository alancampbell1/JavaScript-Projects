Text to Speech Application:

This project demonstrates how to use the Web Speech API (a browser based API).

The user inputs a sentence and through adjusting several settings including the speed of the response, pitch and language, the application will read the input back to the user.

